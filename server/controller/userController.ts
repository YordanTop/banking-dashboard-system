import { Request, Response, NextFunction } from "express";


/** Create user based of the model User  */
export const createUser = (req:Request, res:Response, next:NextFunction) => {

}

/** Update user with new information  */
export const updateUser = (req:Request, res:Response, next:NextFunction) => {

}

/** Select user from the database */
export const selectUser = (req:Request, res:Response, next:NextFunction) => {

}

/** Select all user from the database */
export const selectAllUser = (req:Request, res:Response, next:NextFunction) => {

}


/** Delete user form the database */
export const deleteUser = (req:Request, res:Response, next:NextFunction) => {

}
